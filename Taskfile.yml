# https://taskfile.dev

version: '3'

tasks:
  generate:
    cmds:
      -  protoc -I protos protos/*.proto --go_out=./protos/gen --go_opt=paths=source_relative --go-grpc_out=./protos/gen --go-grpc_opt=paths=source_relative
